package main.server.Contain;

import spark.Request;
import spark.Response;

public class ContainUtil {

    //TODO needs the workout and exercise classes to finish

    public static String selectWorkout(Request request, Response response) {
        String toReturn = "";

        return toReturn;
    }

    public static String selectExercise(Request request, Response response) {
        String toReturn = "";

        return toReturn;
    }
}
